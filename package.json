{
  "name": "aws-lambda-static-ip",
  "version": "1.0.0",
  "description": "This is an example SAM template to make a lambda that executes from a static ip",
  "devDependencies": {
    "aws-sdk": "^2.543.0"
  },
  "scripts": {
    "deploy": "sam build && sam package --output-template-file packaged.yaml --s3-bucket codts && sam deploy --template-file packaged.yaml --stack-name content1-poc-lambda-staticip-stack --capabilities CAPABILITY_IAM"
  },
  "license": "MIT",
  "dependencies": {
    "node_module": "^0.0.0"
  }
}
